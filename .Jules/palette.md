## 2023-10-27 - ARIA labels for icon-only buttons
**Learning:** Icon-only buttons often lack accessible names, making them difficult to use for screen reader users. Adding `aria-label` attributes provides clear context.
**Action:** Always verify that icon-only buttons have descriptive `aria-label` attributes to ensure accessibility.
