## 2024-05-24 - ARIA labels for icon-only buttons
**Learning:** Adding ARIA labels to icon-only buttons (like settings or close buttons) and range inputs significantly improves screen reader accessibility and is an easy win for any interface.
**Action:** Always verify if an interactive element has visible text; if not, supply an aria-label.
